# Unit tests

Unit tests check one component at a time.

In this folder we test:

- ArtistsHandler
- LocationsHandler
- DatesHandler
- RelationsHandler

The tests use controlled data and do not call the real Groupie Tracker API.

This makes it easier to find a problem inside one specific component.
